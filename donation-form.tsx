"use client";import{useState}from"react";import{IndianRupee,LockKeyhole}from"lucide-react";
const presets=[101,501,1001,2001];
export function DonationForm(){const[amount,setAmount]=useState(501);const[msg,setMsg]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setMsg("Development payment adapter is active. A server secret and gateway credentials are required before transactions can be created; no real payment was charged.")}return <form onSubmit={submit} className="card p-5 md:p-8"><p className="text-sm font-bold text-orange-700">CHOOSE AN AMOUNT</p><div className="mt-3 grid grid-cols-2 gap-2 sm:grid-cols-4">{presets.map(v=><button type="button" key={v} onClick={()=>setAmount(v)} className={`rounded-xl border p-3 font-bold ${amount===v?'border-orange-600 bg-orange-50':'border-stone-200'}`}>₹{v.toLocaleString('en-IN')}</button>)}</div><div className="mt-5"><label className="label">Custom amount</label><div className="relative"><IndianRupee className="absolute left-3 top-3.5" size={17}/><input className="input pl-9" type="number" min="1" value={amount} onChange={e=>setAmount(Number(e.target.value))}/></div></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><div><label className="label">Full name</label><input className="input" name="name" required minLength={2}/></div><div><label className="label">Mobile number</label><input className="input" name="mobile" required inputMode="numeric" pattern="[0-9]{10}"/></div><div><label className="label">Email (optional)</label><input className="input" type="email"/></div><div><label className="label">Payment method</label><select className="input"><option>UPI</option><option>Card</option><option>Netbanking</option></select></div></div><div className="mt-4"><label className="label">Address (optional)</label><textarea className="input" rows={2}/></div><button className="btn btn-primary mt-6 w-full" type="submit"><LockKeyhole size={17}/> Continue securely · ₹{amount.toLocaleString('en-IN')}</button>{msg&&<p className="mt-4 rounded-xl bg-amber-50 p-3 text-sm text-amber-900">{msg}</p>}<p className="mt-4 text-center text-xs text-stone-500">A receipt is issued only after server-side payment verification.</p></form>}
