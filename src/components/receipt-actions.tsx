"use client";export function ReceiptActions({receiptNumber}:{receiptNumber:string}){async function share(){const url=location.href;if(navigator.share)await navigator.share({title:`Donation Receipt ${receiptNumber}`,url});else await navigator.clipboard.writeText(url)}return <div className="no-print mx-auto mt-5 flex max-w-[794px] justify-end gap-2"><button className="btn border bg-white" onClick={()=>print()}>Print / PDF</button><button className="btn btn-primary" onClick={share}>Share</button><a className="btn border bg-white" target="_blank" href={`https://wa.me/?text=${encodeURIComponent(`Ganpati donation receipt ${receiptNumber}: `)}${typeof window==='undefined'?'':encodeURIComponent(location.href)}`}>WhatsApp</a></div>}
