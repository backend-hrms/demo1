import{WithdrawalManager}from"@/components/withdrawal-manager";import{createClient}from"@/lib/supabase/server";
export default async function Page(){const sb=await createClient();const{data}=await sb.from("withdrawals").select("id,purpose,amount_minor,status,payment_method,created_at").order("created_at",{ascending:false}).limit(50);return <div className="p-5 md:p-8"><p className="text-sm font-bold text-orange-700">ACCOUNTS</p><h1 className="mt-1 text-4xl">Withdrawals</h1><p className="mt-2 max-w-2xl text-stone-500">Requested → approved → completed. Only completion creates a ledger entry and reduces the balance.</p><WithdrawalManager rows={data??[]}/></div>}
